export default function HomePage(): React.ReactElement {
	return (
		<main style={{ padding: "2rem", fontFamily: "system-ui" }}>
			<h1>Starter</h1>
			<p>
				API en <code>/api</code>. Build móvil: ver scripts <code>build-capacitor</code>.
			</p>
		</main>
	);
}
