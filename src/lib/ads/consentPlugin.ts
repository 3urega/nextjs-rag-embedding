/**
 * UMP / consentimiento (Android): integra aquí el flujo del plugin nativo
 * (p. ej. `@capacitor-community/admob` + User Messaging Platform).
 * Stub para el starter: no-op en web.
 */
export async function requestAdsConsentIfNeeded(): Promise<void> {
	await Promise.resolve();
}
