/**
 * Rewarded AdMob vía Capacitor: implementar según `@capacitor-community/admob`.
 * Devuelve true si el usuario completó el anuncio (stub: false).
 */
export async function showRewardedVideoAndWaitForReward(_adUnitId: string): Promise<boolean> {
	await Promise.resolve();

	return false;
}
